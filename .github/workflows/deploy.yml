name: ci_cd_pipeline_dev
on:
  push:
    branches: [ main, demo, dev, dev2 ]
asd: 
  BAB: hello
env:
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }} 
  FRONTEND_IMAGE_NAME: gcr.io/${{ secrets.GCP_PROJECT_ID }}/epistogram-frontend-${{ asd.BAB }}
  BACKEND_IMAGE_NAME: gcr.io/${{ secrets.GCP_PROJECT_ID }}/epistogram-backend-${{ env.BRANCH_NAME }}
  FRONTEND_CLOUD_RUN_SVC_NAME: epistogram-frontend-${{ env.BRANCH_NAME }}
  BACKEND_CLOUD_RUN_SVC_NAME: epistogram-backend-${{ env.BRANCH_NAME }}
  MIGEN_IMAGE_NAME: gcr.io/${{ secrets.GCP_PROJECT_ID }}/epistogram-migen-${{ env.BRANCH_NAME }}
  CLOUD_SQL_CONNCETION_NAME: gifted-country-324010:europe-central2:epistogram
  GCP_PROJECT_NAME: gifted-country-324010

jobs:

# 
# ------ prerequisites
# 

  job1: 
    name: Hello
    runs-on: ubuntu-latest

    steps:
      - name: Checkout branch
        uses: actions/checkout@v3

      - name: echo
        run: |
          echo ${{ env.BACKEND_IMAGE_NAME }}
      