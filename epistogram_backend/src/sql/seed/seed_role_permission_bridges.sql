INSERT INTO public.role_permission_bridge
(
    id,
    role_id,
    permission_id
)
VALUES
(
    1, -- id
    1, -- role_id,
    1 -- permission_id
),
(
    2, -- id
    1, -- role_id,
    2 -- permission_id
),
(
    3, -- id
    1, -- role_id,
    3 -- permission_id
),
(
    4, -- id
    2, -- role_id,
    4 -- permission_id
),
(
    5, -- id
    2, -- role_id,
    5 -- permission_id
),
(
    6, -- id
    2, -- role_id,
    6 -- permission_id
),
(
    7, -- id
    4, -- role_id,
    7 -- permission_id
);